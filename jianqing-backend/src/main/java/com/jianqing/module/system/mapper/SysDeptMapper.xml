<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jianqing.module.system.mapper.SysDeptMapper">

    <select id="selectAllEnabledDepts" resultType="com.jianqing.module.system.entity.SysDept">
        SELECT id, parent_id, dept_name, leader_user_id, phone, email, sort_no, status, is_deleted
        FROM jq_sys_dept
        WHERE is_deleted = 0
        ORDER BY sort_no ASC, id ASC
    </select>

    <select id="countChildren" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM jq_sys_dept
        WHERE parent_id = #{deptId}
          AND is_deleted = 0
    </select>

</mapper>
