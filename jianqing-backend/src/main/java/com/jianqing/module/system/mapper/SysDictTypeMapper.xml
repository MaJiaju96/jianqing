<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jianqing.module.system.mapper.SysDictTypeMapper">

    <select id="selectAllDictTypes" resultType="com.jianqing.module.system.entity.SysDictType">
        SELECT id, dict_name, dict_type, status, remark, created_by, updated_by, created_at, updated_at
        FROM jq_sys_dict_type
        ORDER BY id ASC
    </select>

    <select id="countByDictType" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM jq_sys_dict_type
        WHERE dict_type = #{dictType}
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>

</mapper>
