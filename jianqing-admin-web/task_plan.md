# Task Plan: 简擎前端管理台（v0.1）

## Goal
构建一个可直接联调 `jianqing-backend` 的前端管理台，界面不死板、交互流畅，并保持纯 `JavaScript` 实现。

## Current Phase
Phase 43

## Phases

### Phase 1: 需求与风格对齐
- [x] 明确前后端目录平级
- [x] 明确前端使用纯 JavaScript
- [x] 明确页面风格目标（现代感、非死板）
- **Status:** complete

### Phase 2: 脚手架与基础能力
- [x] 创建 Vite + Vue3 + Element Plus 项目
- [x] 配置 `/api` 代理到后端
- [x] 接入登录态与路由守卫
- **Status:** complete

### Phase 3: 页面落地
- [x] 登录页
- [x] 仪表盘
- [x] 系统管理页（用户/角色/菜单）
- [x] 审计日志页（操作/登录）
- **Status:** complete

### Phase 4: 质量与体验优化
- [x] 全量去 TypeScript
- [x] 前端构建验证通过
- [x] 补充前端 README（概要/启动/联调）
- [x] 菜单权限页层级与类型可读性优化
- [x] 所有列表页统一分页交互
- **Status:** complete

### Phase 5: v0.2 数据权限配置起步
- [x] 在角色页展示数据范围字段
- [x] 在角色表单支持编辑数据范围
- [x] 补齐部门管理页面与路由接入
- [x] 补一轮数据范围联调验证
- [x] 做一轮整体 UI 细节巡检（表格列宽/分页信息/按钮文案/空态文案）
- **Status:** complete

### Phase 6: 代码健康治理（列表页去重复）
- [x] 识别系统管理四个主列表页重复状态流
- [x] 抽离系统列表通用 composable（users/roles/menus/depts）
- [x] 完成四个系统页接入并保留原有交互行为
- [x] 评估并抽离系统页 create/edit 弹窗表单轻量 composable
- [x] 抽离系统页删除确认与删除后反馈轻量 composable
- [x] 抽离系统页保存提交与成功反馈轻量 composable
- **Status:** complete

### Phase 7: 前端架构收口与审美约束固化
- [x] 抽离系统页部门树公共工具（options/nameMap/tree flatten/filter）
- [x] 抽离菜单类型元数据公共工具（type text/tag/filter）
- [x] 抽离共享列表头部组件并接入系统页/审计页
- [x] 保持列表页统一骨架与交互节奏，完成构建验证
- [x] 同步更新前端规划文件与统一开发规范，固化后续约束
- **Status:** complete

### Phase 8: CRUD 代码生成器前端最小闭环
- [x] 对齐后端 `dev/gen` 元数据接口结构与字段语义
- [x] 设计代码生成器页最小交互（选表 / 看字段 / 预览 / 下载）
- [x] 复用现有列表页头部与反馈节奏，避免新页面风格分叉
- [x] 接入前端 API / 路由 / 页面骨架并完成构建验证
- [x] 补齐生成器页预览说明区，降低首次使用门槛
- [x] 补齐字段/模板注意事项提示，帮助生成前快速自检
- [x] 完成生成器页说明增强后的真实浏览器回归，并修正产物分类统计
- [x] 重排生成器双栏布局，将注意事项改为告警按钮 + 抽屉查看，优先保证字段表格可读性
- [x] 收紧生成器工具栏与代码预览区排版，避免工具栏乱换行并增强代码滚动查看体验
- [x] 继续优化生成器顶部工具区层次，将输入与动作区改为更明确的卡片化结构
- [x] 补齐预览文件用途说明，帮助切换模板时快速理解当前文件职责
- [x] 补齐预览文件复制能力，支持复制当前文件路径与内容
- [x] 补齐代码预览阅读模式切换，支持自动换行与保留长行
- **Status:** complete

### Phase 9: 代码生成器参数校验收口
- [x] 对齐后端参数规则并在前端补齐命名格式校验提示
- [x] 统一生成器请求参数透传前的 trim 规范化，避免输入态噪音下发
- [x] 完成预览/下载/写入链路回归验证
- **Status:** complete

### Phase 10: 代码生成写入安全交互
- [x] 写入失败时识别“文件冲突”语义并提示覆盖风险
- [x] 增加覆盖确认弹窗，确认后显式带 `overwrite=true` 重试
- [x] 完成生成器写入链路构建与交互回归
- **Status:** complete

### Phase 11: 冲突文件清单可视化确认
- [x] 写入前先查询冲突文件清单，不再依赖失败后重试
- [x] 在覆盖确认弹窗展示冲突文件列表（含数量与截断）
- [x] 完成冲突清单查询 + 覆盖写入链路构建回归
- **Status:** complete

### Phase 12: 冲突清单面板增强
- [x] 将冲突确认从纯文本弹窗升级为可滚动清单面板
- [x] 支持一键复制冲突文件清单，便于离线评估
- [x] 完成写入覆盖确认面板交互与构建回归
- **Status:** complete

### Phase 13: 冲突清单分组与检索
- [x] 按目录对冲突文件分组展示，提升大批量冲突可读性
- [x] 增加冲突路径关键字搜索与筛选计数反馈
- [x] 复制动作支持按当前筛选结果导出
- [x] 完成分组检索交互与构建回归
- **Status:** complete

### Phase 14: 冲突高风险目录快捷过滤与折叠浏览
- [x] 增加冲突目录快捷过滤（全部 / 高风险 Java / 前端 / SQL）
- [x] 支持按目录分组折叠/展开，降低长清单阅读负担
- [x] 完成快捷过滤 + 折叠交互与构建回归
- **Status:** complete

### Phase 15: 冲突差异视图与目录摘要
- [x] 增加“仅文件名（忽略路径）”冲突查看模式
- [x] 增加目录统计摘要条，快速感知冲突分布
- [x] 复制清单动作对齐当前显示模式（路径/文件名）
- [x] 完成差异视图与目录摘要构建回归
- **Status:** complete

### Phase 16: 冲突风险排序与批量折叠控制
- [x] 冲突分组按风险优先级排序（Java > SQL > 前端 > 其他）
- [x] 增加“一键展开/折叠全部分组”操作
- [x] 完成风险排序与批量折叠交互构建回归
- **Status:** complete

### Phase 17: 冲突清单导出能力
- [x] 在冲突确认面板新增 TXT 导出动作
- [x] 导出内容对齐当前显示模式与筛选结果
- [x] 完成导出动作与构建回归
- **Status:** complete

### Phase 18: 冲突清单 Markdown 导出
- [x] 新增冲突清单 Markdown 导出动作
- [x] 导出内容包含过滤条件、目录统计与冲突明细
- [x] 完成 Markdown 导出与构建回归
- **Status:** complete

### Phase 19: Markdown 导出风险标签增强
- [x] 为冲突明细补充风险级别标签（HIGH/MEDIUM/LOW）
- [x] 在导出头部补充风险分布统计
- [x] 完成风险标签导出构建回归
- **Status:** complete

### Phase 20: Markdown 覆盖建议顺序
- [x] 新增“覆盖建议顺序”章节
- [x] 按风险与目录输出建议复核优先级
- [x] 完成建议顺序导出构建回归
- **Status:** complete

### Phase 21: Markdown 导出前预览
- [x] 在冲突确认面板新增 Markdown 预览入口
- [x] 提供预览弹窗内复制能力，便于先审阅后导出
- [x] 完成预览交互与构建回归
- **Status:** complete

### Phase 22: 生成标记展示与快速删除入口
- [x] 写入成功反馈显示 markerId，并本地记录最近标记历史
- [x] 在“更多”菜单新增按标记快速删除入口
- [x] 支持历史选择与手输 markerId 删除，并完成构建回归
- **Status:** complete

### Phase 23: 写入记录后端化
- [x] 快速删除标记列表改为优先读取后端写入记录接口
- [x] 保留本地缓存作为后端接口不可用时的回退方案
- [x] 保持“未写入项目”的配置历史仅存本地缓存
- [x] 完成构建回归
- **Status:** complete

### Phase 24: 写入记录列表页（筛选 + 一键删除）
- [x] 在生成器页新增“写入记录”弹窗入口
- [x] 支持按表名与时间范围过滤写入记录
- [x] 支持在记录列表中按 marker 一键删除并刷新
- [x] 完成前后端构建/测试回归
- **Status:** complete

### Phase 25: 字典管理前端最小闭环
- [x] 新增字典管理路由、侧边栏入口与系统 API 封装
- [x] 实现字典类型列表与字典数据列表双区块管理页
- [x] 支持字典类型/数据新增、编辑、删除与类型切换联动查询
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 26: 字典业务下拉首批接入
- [x] 抽离前端字典 options 读取 composable
- [x] 用户/角色/菜单/部门页状态相关下拉与展示改为读取字典项
- [x] 菜单页可见性改为读取字典项，去掉对应硬编码文案
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 27: 审计页字典消费接入
- [x] 登录日志页接入审计执行状态与登录方式字典
- [x] 操作日志页接入审计执行状态字典
- [x] 去除审计页成功/失败、密码等硬编码筛选文案来源
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 28: 参数设置前端最小闭环
- [x] 新增参数设置路由、侧边栏入口与系统 API 封装
- [x] 实现参数列表、搜索筛选与新增/编辑/删除弹窗
- [x] 对齐参数键和值类型前端校验与内置参数交互限制
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 29: 参数分组前端最小闭环
- [x] 参数页新增分组列与分组筛选
- [x] 参数新增/编辑弹窗支持维护分组字段
- [x] 分组输入支持复用已有分组与按需创建
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 30: 参数变更历史前端最小闭环
- [x] 参数页新增历史查看入口
- [x] 历史弹窗展示变更类型、分组、参数值、说明与时间
- [x] 接入后端参数历史接口并完成构建回归
- [x] 同步规划文件
- **Status:** complete

### Phase 31: 参数版本回滚前端最小闭环
- [x] 历史弹窗新增回滚操作
- [x] 禁止对删除态历史执行直接回滚
- [x] 回滚后刷新参数列表与历史列表
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 32: 参数 diff 对比前端最小闭环
- [x] 历史弹窗新增“对比”入口
- [x] 展示当前参数与指定历史快照的字段级差异
- [x] 高亮是否变化，便于快速判断是否需要回滚
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 33: 参数删除恢复前端最小闭环
- [x] 参数页新增“恢复已删”入口与已删历史弹窗
- [x] 支持查看当前仍未恢复的删除记录并按历史恢复
- [x] 恢复成功后刷新参数列表与已删记录列表
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 34: 参数多版本 diff 前端最小闭环
- [x] 历史弹窗新增“设为基准”能力
- [x] 支持基于两条历史记录发起 diff 对比
- [x] diff 弹窗按对比模式展示“当前 vs 历史”或“历史 vs 历史”文案
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 35: 参数删除恢复前预览前端最小闭环
- [x] 已删记录列表新增“预览”入口
- [x] 恢复前预览弹窗展示待恢复参数关键字段
- [x] 预览弹窗支持确认恢复并联动刷新列表
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 36: 参数恢复前差异对比前端最小闭环
- [x] 恢复预览弹窗展示同键占用状态
- [x] 同键已存在时展示“当前同键值 vs 待恢复值”字段级差异
- [x] 同键已存在时禁用确认恢复按钮
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 37: 参数值类型字典消费扩面
- [x] 参数页值类型筛选改为优先消费字典 options
- [x] 参数新增/编辑弹窗值类型改为优先消费字典 options
- [x] 值类型展示文案改为走字典 label，并保留兜底映射
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 38: 字典颜色类型字典消费扩面
- [x] 字典管理页颜色下拉改为优先消费字典 options
- [x] 字典数据列表颜色展示改为字典文案 + 标签类型
- [x] 保留前端兜底颜色选项，避免旧环境不可用
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 39: 参数来源字典消费扩面
- [x] 参数页来源筛选改为优先消费字典 options
- [x] 参数新增/编辑弹窗来源选项改为优先消费字典 options
- [x] 来源展示文案与标签类型改为走字典配置，并保留兜底映射
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 40: 字典管理页状态字典消费扩面
- [x] 字典类型/字典数据状态筛选改为优先消费 `sys_common_status`
- [x] 字典类型/字典数据表单状态选项改为优先消费字典 options
- [x] 状态标签文案与颜色改为字典驱动，并保留前端兜底
- [x] 完成构建回归并同步规划文件
- **Status:** complete

### Phase 41: 字典与参数页真实回归收口
- [x] 完成字典管理真实浏览器回归，覆盖新增类型/新增数据/删除链路
- [x] 修复字典页颜色标签在默认值场景下的控制台 warning
- [x] 完成参数设置真实浏览器回归，推动后端修复已删参数恢复链路问题并复测通过
- **Status:** complete

### Phase 42: 参数多历史与生成器写入回滚真实回归
- [x] 完成参数页双历史 diff 与回滚真实浏览器回归
- [x] 完成生成器写入记录删除真实浏览器回归
- [x] 验证写入后 marker 删除成功且页面无 warning
- **Status:** complete

### Phase 43: 生成器冲突确认与系统筛选边界真实回归
- [x] 完成生成器冲突清单 / 快捷过滤 / 取消覆盖真实浏览器回归
- [x] 修复生成器冲突弹窗 `el-radio` 兼容 warning
- [x] 完成字典页与参数页筛选 / 重置 / 每页条数边界真实浏览器回归
- **Status:** complete

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| 前端目录与后端平级 | 便于独立启动、构建、部署 |
| 前后端统一放置在 `code/jianqing` 工作区 | 便于单目录开启跨端协作开发 |
| 前端默认纯 JavaScript | 满足当前开发偏好，降低改动成本 |
| UI 采用玻璃态 + 渐变风格 | 避免传统后台“死板感” |
| 关键/复杂逻辑必须写注释 | 提高团队协作与后续维护效率 |
| 前端实现坚持极简可读 | 保证低学习成本和快速接手能力 |
| 兼容后续多前端分支演进 | 为 `TS` 分支与 `Vue2` 分支预留迁移空间 |
| 前后端接口方法统一为 `GET` / `POST` | 降低联调歧义，避免 `PUT` / `DELETE` 方法分歧 |
| 列表页统一分页交互 | 降低长列表认知负担，提升信息检索效率 |
| 菜单页增加类型筛选与层级信息 | 强化菜单/按钮权限边界与结构可读性 |
| 前端公共约定收敛到规范文档与常量模块 | 降低 AI/人工协作时的隐式约定理解成本 |
| 列表高度布局统一采用 `flex` 分配 | 避免持续调 `offset` 这类魔法参数，减少样式补丁累积 |
| 列表页头部工具栏统一走共享组件 | 保持系统页与审计页操作骨架一致，减少样式和交互漂移 |
| 树工具与菜单元数据优先抽公共层 | 避免重复逻辑重新散落回视图页，保持页面脚本轻量 |
| 代码生成器页先做“元数据查看 + 预览/下载” | 先验证产品闭环与模板输出，避免首版引入复杂在线编辑器 |

## Sync Rules
- 开发联调测试账号：`admin/admin123`；`dept_user/test123`；`self_user/test123`；`other_user/test123`；`outside_user/test123`。
- 前后端均维护独立 `task_plan.md / findings.md / progress.md`。
- 发生跨项目变更时，两个目录的计划文件都要同步记录。
- 注释执行规则：仅在关键逻辑/复杂流程处添加必要注释，避免无意义注释噪音。
- 前端编码原则：优先简单直接实现，避免复杂抽象。
- 协作原则：命名与结构保持清晰，确保 AI 与开发者都可快速理解。
- 接口联调规则：前端 API 调用仅使用 `GET` 与 `POST`，不使用 `PUT` / `DELETE`。
- 可读性规则：优先通过常量、函数命名与结构收敛提升可读性，避免用大量解释性注释替代清晰代码。
- 布局规则：列表页禁止新增“视口减常量”式高度计算，统一使用“工具区 + 表格区(填充) + 分页区(贴底)”结构。
- 共享层规则：同类列表页重复出现第 2 次时，必须评估是否抽成 composable / utils / 共享组件。
- 审美约束：新增页面与重构页面必须优先复用统一列表骨架与反馈节奏，避免“功能可用但风格分叉”。
