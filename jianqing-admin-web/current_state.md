# Current State

## 当前阶段
- Phase 51 进行中：系统级消息通知前端进入信息架构与最小闭环实现阶段。

## 最近完成
- 已完成运行中联调烟测：本地前端 `127.0.0.1:5173` 与后端 `127.0.0.1:8080` 已同时可用，并实测管理员通知链路覆盖登录、发布、未读数变化、单条已读、全部已读、垃圾箱删除/恢复/彻底删除。
- 已补退出登录通知清理：头部通知下拉在未登录态会立即清空未读数/最近消息，通知流断开时也会同步重置首页弹窗，避免退出后残留旧通知状态，`npm run build` 通过。
- 已补头部通知局部容错：未读数/最新消息与弹窗候选的并行拉取已改为部分成功可落地，单个接口抖动不会让整个头部通知区或首页弹窗一起失效，`npm run build` 通过。
- 已补通知管理目标选项解耦：通知列表加载与角色/部门/用户目标选项拉取已拆开，目标选项接口异常时不再拖垮整个管理列表，新建/编辑时也会继续尝试补拉选项，`npm run build` 通过。
- 已补消息详情空态与返回失效：详情页在路由参数缺失、用户返回列表或请求失败时会立即作废旧请求并展示空态，避免离开后迟到响应回写旧正文，`npm run build` 通过。
- 已补全局通知手动关闭静默：首页待处理通知弹窗右上角关闭会按“五分钟不再弹窗”处理，避免用户手动关闭后被路由切换或后续刷新立刻重新弹出，`npm run build` 通过。
- 已补通知管理弹窗关闭失效：管理页详情/编辑弹窗在用户手动关闭后会立即作废进行中的旧请求并清空旧数据，避免关闭后被迟到响应重新回写，`npm run build` 通过。
- 已补我的消息批量已读容错：批量已读改为并发受控的逐条处理，单条失败不会拖垮整批操作，并会分别提示成功/失败条数，`npm run build` 通过。
- 已补通知管理详情/编辑防串单：管理页快速切换不同通知详情或编辑对象时会丢弃过期响应，失败时同步清空旧详情/表单，避免弹窗内容回写错位，`npm run build` 通过。
- 已补通知目标切换清理：通知管理新建/编辑弹窗在角色/部门/用户/全员之间切换时会清空旧目标 ID，避免跨目标类型残留错误选项，`npm run build` 通过。
- 已补消息详情请求防串单：详情页切换不同通知或请求失败时会丢弃过期响应并清空旧详情，避免快速切换后正文回写错位，`npm run build` 通过。
- 已补本地列表分页回退：`useListPage` 会在筛选结果、删除操作或页容量变化后自动收敛到有效页码，避免最后一页删空后表格空白但总数仍大于 0，`npm run build` 通过。
- 已补通知弹窗并发防抖：首页弹窗候选拉取加入失效令牌，登录态切换、路由切换、静默期内不会被旧请求回写，`npm run build` 通过。
- 已补首页弹窗实时补拉：SSE 收到通知刷新事件后会主动重拉弹窗候选，避免用户停留在非消息页时只刷角标不弹出新通知，`npm run build` 通过。
- 已补通知 SSE 前端容错：`subscribeMyNoticeStream` 现在会兜住异常事件解析，避免单条脏事件中断整条实时链路，`npm run build` 通过。
- 已补消息中心体验收边：我的消息批量已读/详情已读会主动触发头部通知刷新，长正文摘要与全局弹窗长标题展示已优化，`npm run build` 通过。
- 已完成通知交互增强提交并推送：`f800125 完善系统通知交互并补齐垃圾箱能力` 已推送至 `origin/master`。
- 已补齐消息中心交互增强：我的消息支持批量已读/排除紧急，通知管理支持批量发布/删除、垃圾箱恢复/彻底删除。
- 已完成全局通知弹窗增强：支持跨页面检查、多条分页浏览、未读总数展示、内容摘要、设置已读、五分钟静默。
- 已补齐头部铃铛实时刷新：基于 SSE 长连接更新未读数与最近消息，并在管理页操作后主动触发刷新桥接。
- 已统一通知相关页面时间显示格式：前端展示统一为 `YYYY-MM-DD HH:mm:ss`，不再显示 `T`。
- 已完成默认端口联调复核：前端开发服务 `127.0.0.1:5173` 可访问首页、`/#/messages/mine`、`/#/messages/manage`；后端 SSE 与通知接口均已可用。
- 后端已补齐消息中心菜单与 `system:notice:*` 权限种子，前端通知管理页的细粒度按钮显隐已有真实权限来源。
- 默认端口联调已确认：管理员菜单树可见 `消息中心 -> 我的消息/通知管理`，并拿到完整通知管理权限。
- 已新增消息中心路由骨架：`我的消息`、`通知管理`、`消息详情` 三个页面已接入。
- 已接入头部铃铛未读数与最近消息下拉，支持从任意页进入消息详情。
- 首页已接入首次会话提醒弹窗，支持跳转消息中心或直接进入单条详情。
- 前端通知页面首版构建通过：`npm run build`。
- 侧边栏已拆为 `系统管理`、`参数管理`、`开发工具`、`审计日志` 四组。
- `系统管理` 仅保留用户/部门/角色/菜单；参数/字典已归入 `参数管理`，代码生成已归入 `开发工具`。
- 前端最新构建通过：`npm run build`。
- 角色页已支持“自定义部门数据”选项，并复用部门树完成勾选配置。
- 角色列表接口现已携带 `customDeptIds`，支持编辑态回填自定义部门勾选。
- 角色页已支持“本部门及以下数据”选项与正确文案展示。
- 已完成 `dept_user` 在真实前端用户页的回归：可见子部门用户 `outside_user`。
- 本轮用户页真实回归后控制台 `warning/error = 0`。

## 当前基线
- `dept_scope_role` 已恢复为“本部门数据”，联调基线未污染。
- 前端当前主线稳定：系统管理、参数管理、开发工具、审计、数据权限扩展、消息中心与通知交互增强均已打通。
- 新需求基线已确认：通知入口已覆盖全局弹窗/铃铛/消息列表/详情/垃圾箱，当前重点转向真实体验联调与细节收口。

## 下一步最小任务
1. 用真实通知数据补一轮浏览器级联调，确认弹窗分页、实时角标、垃圾箱切换与恢复链路表现。
2. 重点复核本轮主动刷新桥接是否覆盖“列表批量已读 / 详情自动已读 / 管理页发布删除恢复 / 非消息页实时弹窗”四类场景。
3. 如主链路稳定，可继续补普通角色菜单分配策略或消息统计视图。

## 关键约束
- 普通前端任务优先只读取本文件；仅在需要历史细节时再展开 `backlog.md / decisions.md / task_plan.md / findings.md / progress.md`。
- 前端继续遵循极简可读、统一列表骨架、避免无关抽象增加上下文负担。
