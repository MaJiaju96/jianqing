# Findings & Decisions

## Requirements
- 前后端必须是平级目录
- 前端尽量使用纯 JavaScript
- 页面设计不要死板，具备现代感
- 项目整体坚持极简、易读、可演进
- 代码需保证后续 AI 持续开发友好
- 需预留前端 `TS` 分支与 `Vue2` 分支演进空间

## Research Findings
- 后端接口已具备登录、系统管理、审计日志能力，可直接联调。
- Vite 代理 `/api -> http://127.0.0.1:8080` 可满足本地联调。
- 当前前端已无 `.ts` 文件，且 `npm run build` 通过。
- 通知前端首版最稳入口已验证为“左侧消息中心菜单 + 顶部铃铛 + 首页首次提醒”三位一体，日常浏览走铃铛/消息列表，强提醒走 dashboard 会话弹窗。
- `useSystemListPage` 当前更适合单一 filter 值；通知页这类双筛选场景需在页面层维护独立输入 ref，并在搜索时重新赋值 `filterInput.value = {...}`，否则对象引用会导致筛选态提前联动。
- 通知管理页当前应按按钮级权限区分 `add/edit/publish/cancel/delete`，不能只用 `system:notice:list` 控制整页，否则操作区会过度放开。
- 已新增 `README.md`，覆盖项目概要、启动命令、构建命令、代理联调与常见问题。
- 项目已迁移至统一工作区：`/Users/majiaju/Person/code/jianqing`。
- 菜单管理页已从树列表改为树形表格，支持类型区分（目录/菜单/按钮）。
- 角色分配菜单已支持按“菜单权限/按钮权限”筛选，便于精确分配按钮权限。
- 侧边栏与路由已按权限标识显隐/拦截，审计日志不再默认对无权角色展示。
- 已将系统管理 API 写操作由 `http.put/http.delete` 统一迁移为 `http.post`，前端联调接口方法统一为 `GET`/`POST`。
- 菜单管理页已增加类型筛选（全部/目录菜单/按钮）与层级展示，菜单与按钮权限关系更清晰。
- 所有列表页面已统一接入分页（用户、角色、菜单、操作日志、登录日志）。
- 用户/角色/审计日志页已补齐“分野筛选”（状态/登录方式/关键字）并与分页联动。
- Element Plus 已切换中文本地化，分页统一展示中文“页”语义。
- 已新增 `docs/FRONTEND_CONVENTIONS.md`，沉淀前端注释规则、常量收敛、AI 协作与公共层约定。
- 已新增 `src/constants/app.js`，统一收敛成功码、分页、状态、菜单类型与超级管理员角色等公共常量。
- 已新增 `src/utils/validators.js`，统一收敛手机号、邮箱、角色编码、权限标识校验逻辑。
- 已完成一轮前端可读性整理：减少视图层魔法值、收敛重复校验、补齐 README 对规范文档的入口。
- 已新增 `src/composables/usePermissions.js`，统一收敛页面批量权限显隐的 computed 模式。
- 已完成一轮前端构建体积优化：路由改为懒加载，Element Plus 改为按需组件解析，消除大 chunk 构建告警。
- 已补齐站点 favicon，并统一关键列表页的加载中/暂无数据文案与保存、删除按钮 loading 反馈。
- 已新增 `src/composables/useAsyncState.js`，统一列表页加载态、按钮提交态与行级删除 loading，减少页面重复异步反馈代码。
- 已新增 `src/utils/feedback.js`，统一登录、登出及系统管理写操作成功提示文案，并为审计查询输入增加回车触发。
- 页面巡检发现 Element Plus locale 在按需引入改造后回退为英文，已通过 `App.vue` 的 `ElConfigProvider` 恢复中文分页/下拉等文案。
- 已启动 v0.2 数据权限前端收口：角色页增加“数据范围”展示与编辑，当前与后端最小闭环保持一致，仅支持全部/本部门/本人三种范围。
- 已补齐部门管理页面：前端新增 `DeptsView`、系统 API 与侧边栏路由接入，部门管理不再只是 seed 菜单项。
- 已将用户管理里的“部门ID”输入升级为真实部门下拉，并在列表中显示所属部门名称，降低对纯数字 ID 的依赖。
- 已将部门管理里的“负责人ID”升级为真实用户选择，并在列表中显示负责人姓名（昵称 + 用户名）。
- 已统一用户页与部门页筛选交互：搜索词与状态筛选均改为“查询 / 重置”模式，支持回车触发查询。
- 已补齐列表区自适应固定高度能力：系统页与审计页表格统一改为随视口高度计算 `max-height`，数据超出时走表格内部滚动。
- 已修复部门页弹窗可能被页面内容遮挡的问题：关键弹窗统一追加到 body 渲染。
- 部门页已补齐分页，且分页后去掉树展开按钮，改为层级缩进展示，避免分页与树展开混用造成视觉混乱。
- 已统一角色页与菜单页筛选交互，系统管理四个主列表页现均采用一致的“查询 / 重置”模式。
- 已统一审计页筛选交互，操作日志与登录日志现也采用显式“查询 / 重置”模式，前后台列表页筛选心智已基本一致。
- 已将所有查询列表从“根据数据量伸缩”收敛为“固定占满可用工作区高度”：表格统一使用 `height` 而非 `max-height`，空数据时也保持稳定骨架。
- 已补齐页面宽高响应式适配：`MainLayout` 主内容区允许内部滚动，列表卡片高度随视口变化；工具栏在窄屏下自动换行，小屏时控件自动纵向铺开。
- 已完成 768 / 640 宽度巡检：系统与审计列表页无主内容横向溢出；移动端分页区补齐左对齐与横向滚动，避免窄屏下分页控件拥挤。
- 已完成最新一轮真实账号联调复核：角色页数据范围列与编辑值正常；部门页列表、分页与负责人展示正常；`dept_user` 仅见本部门用户，`self_user` 仅见自己。
- 当前开发联调测试账号口径已统一：`admin/admin123`，其余测试账号统一使用 `test123`。
- 已将系统页与审计页列表统一为“表格卡片 + 独立分页卡片”双区块布局，表格与分页行视觉彻底分离，页面层次更清晰。
- 已进一步重构列表页顶部工具区：移除冗余页面标题，改为左侧筛选/查询、右侧刷新/新增的模块化布局，更贴近管理台内容区操作习惯。
- 已为列表工具栏补齐主题变量：工具栏面板、输入框、普通按钮、刷新按钮、表格面板、分页面板均随主题联动，不再只有白色块感。
- 已完成列表页高度逻辑重构：移除 `useAdaptiveTable` 与全局 `offset` 调参方案，统一改为 `flex` 布局分配高度（分页贴底、表格区 `height=100%` 填充剩余空间）。
- 已完成一轮前端“屎山扫描”并确认高重复区：审计页（操作日志/登录日志）分页、查询、重置、刷新与加载状态逻辑高度重复。
- 已新增 `useAuditListPage` 组合式工具，收敛审计页重复分页查询状态，减少页面样板逻辑并统一交互节奏。
- 已新增 `src/composables/useSystemListPage.js`，统一系统管理四个主列表页（users/roles/menus/depts）的查询、重置、刷新、分页状态流，页面仅保留领域差异逻辑。
- 已新增 `src/composables/useEntityDialogForm.js`，统一系统管理四个主弹窗表单的可见性、编辑态、编辑目标、表单初始化与编辑回填，页面继续保留校验与提交 API。
- 已新增 `src/composables/useEntityDeleteAction.js`，统一系统管理四个主列表页删除确认、行级 loading、删除后刷新与成功提示，页面仅传入实体标签、展示字段与删除 API。
- 已新增 `src/composables/useEntitySubmitAction.js`，统一系统管理四个主弹窗表单保存时的 loading、关闭弹窗、刷新列表与成功提示，页面继续保留字段校验与 create/update 分支。
- Playwright 真实回归发现两处轻量抽象回归点：`openCreate` 会收到原生点击事件对象、删除取消会产生未处理 `cancel`；现已分别在 `useEntityDialogForm` 与 `useEntityDeleteAction` 修复。
- 已补做数据权限账号回归：浏览器上下文验证 `dept_user` 可见 `admin/test/dept_user/self_user/other_user`，`self_user` 仅见 `self_user`；并查清 `outside_user` 当前因挂载 `Test_User(dataScope=ALL)` 而可见 6 人，属于配置现状。
- 已完成审计页真实回归：`/audit/oper-logs` 与 `/audit/login-logs` 的查询、重置、分页流程正常，当前两页各展示 20 条数据。
- 已新增 `src/utils/errors.js` 中的 `ignoreHandledError`，并接入 system/audit/dashboard 相关页面与列表 composable，用于吞掉已由 HTTP 拦截器弹出的失败请求，减少控制台未处理 Promise 警告噪音。
- 已修复非 admin 账号登录后的 dashboard 噪音：`src/views/DashboardView.vue` 不再无差别请求 users/roles/menus，而是按 `hasPerm` 逐项拉取；无权限统计显示为 `--`。
- 已完成系统页部门树工具抽离：新增 `src/views/system/deptTreeUtils.js`，统一收敛 dept options、nameMap、flatten rows 与 tree filter。
- 已完成系统页菜单元数据抽离：新增 `src/views/system/menuMeta.js`，统一收敛菜单类型文本、tag 与类型筛选判断。
- 已完成列表页头部工具栏共享化：新增 `src/components/ListPageHeader.vue`，系统页与审计页共 6 个列表页已统一接入。
- 已将“第二次重复就评估抽共享层”“共享组件优先承载外壳结构与固定节奏”“保持统一列表骨架和交互美感”等规则同步进统一开发规范，用于约束后续开发。
- 已完成 Dashboard 统计加载逻辑收口：新增 `src/composables/useOverviewCounts.js`，统一承载权限判断、统计请求、菜单节点计数与失败兜底展示。
- 已完成页面初始化收口：新增 `src/composables/usePageInitializer.js`，统一 `onMounted + try/catch + ignoreHandledError + 初始加载` 的固定节奏。
- 已完成状态标签收口：新增 `src/components/StatusTag.vue`，统一启用/禁用、成功/失败、停用等状态标签展示语义。
- 已完成列表页基础层统一：新增 `src/composables/useListPage.js`，`useAuditListPage` / `useSystemListPage` 均已降为轻 wrapper，分别承载远程分页列表与本地筛选列表语义。
- 已进入 CRUD 代码生成器主线的前端准备阶段：后端已先提供 `dev/gen` 元数据接口，前端下一步只需围绕“选表 / 字段查看 / 预览 / 下载”做最小页面闭环。
- 代码生成器页首版不应偏离当前管理台骨架，建议继续复用 `ListPageHeader`、统一按钮反馈与现有路由 `meta.perm` 约定。
- 已新增 `src/api/generator.js`，其中 JSON 接口继续复用 `http` 拦截器，ZIP 下载则改用原生 `axios + blob`，避免被统一 `ApiResponse` 拦截逻辑误处理。
- 已新增 `src/views/system/GeneratorView.vue`，首版采用“双栏布局：字段元数据 + 代码预览 tabs”，并继续复用 `ListPageHeader`、全局反馈与主题样式。
- 已在 `router/index.js` 与 `MainLayout.vue` 补齐生成器路由与系统菜单入口，权限标识使用 `system:generator:list`。
- 生成器页当前只做构建验证，尚未补浏览器真实回归；若继续收口，应优先补一轮下载与表切换 smoke 测试。
- 浏览器回归时发现 `MainLayout.vue` 首次接入“代码生成”菜单后，脚本层遗漏了 `canViewGenerator` 权限计算，已补齐，避免 Vue runtime warning。
- 在后端不可联通的情况下，已使用 Playwright mock 跑通生成器页 smoke：登录 → 进入 `/system/generator` → 选表 → 查询预览 → 触发 ZIP 下载，页面主链路正常。
- 当前已补做真实浏览器 smoke：使用真实后端登录后进入 `/system/generator`，成功加载表列表、选中 `jq_sys_dept`、生成 9 份预览文件并触发 ZIP 下载。
- 本轮真实回归未发现新的生成器页运行错误；控制台仅出现一次登录页初始化阶段的 `/api/auth/me` 401 噪音，不影响生成器链路本身。
- 根据最新产品预期，生成器不应只产出后端与权限 SQL；当前后端模板已继续补齐前端 API 模板、前端 CRUD 列表页模板与路由片段输出。
- 当前生成器产出的前端模板精度已进一步提升：日期/时间、长文本备注、布尔开关、数值输入等控件会按字段类型自动分配，不再全部退化成普通输入框。
- 已在生成器页预览区补齐“当前表 / 产物分布 / 使用步骤”说明，首次进入页面时也能快速理解先看什么、再下载什么。
- 已在字段元数据区补齐“字段/模板注意事项”提示，可根据主键、状态字段、软删字段、时间字段、长文本字段给出生成前自检建议。
- 已完成说明增强后的真实浏览器回归：生成器页说明区、字段注意事项、选表、预览、下载链路均正常；过程中发现预览产物分类统计首版误把部分前后端文件计入“其他”，现已按真实路径规则修正为“后端 8 / 前端 3 / SQL 1”。
- 结合本地代码模式与 Element Plus 常见做法后，生成器页已将常驻注意事项块改为“告警按钮（WarningFilled） + 抽屉”交互：字段区顶部仅保留当前表/字段数摘要，主空间优先留给字段表格观察。
- 进一步收口后，生成器工具栏改为“配置网格 + 独立动作行”结构，不再把查询输入与按钮硬塞进同一行导致随机换行；代码预览区也已从 `textarea` 改为暗色等宽字体的滚动代码视图，优先保证代码可连续阅读。
- 顶部工具区继续做了视觉层次增强：将“生成配置”和“快捷操作”拆成两个卡片区块，并为每个输入补齐字段标签、为当前表补充 badge 展示，顶部不再像临时拼接的表单条。
- 在继续回看后确认“双卡片”方案视觉负担偏重，已回退到更克制的统一工具栏：保留标题说明、当前表 badge、字段 label 与右侧动作簇，但移除额外卡片分割，整体更贴近当前后台壳层的轻量风格。
- 进一步精简后，右上角表名 badge 已去掉，顶部改为“左侧标题说明 + 右侧按钮组”同一水平对齐，按钮垂直居中；由于选表本身已在输入区完成，去掉 badge 后信息密度更合适。
- “注意事项”按钮已从顶部主操作区迁回字段上下文，放到“字段元数据”标题行最右侧。这样顶部只保留生成相关动作，提醒信息归位到字段区域，信息归属更自然。
- 字段区的提醒入口已进一步收敛为圆角更柔和的图标按钮，仅保留三角叹号图标并通过 tooltip 提示“查看注意事项”，更符合“上下文提示”而不是“功能按钮”的角色。
- 提醒内容已从“半静态建议”继续升级为“按表结构动态生成”：当前会根据是否有主键、是否缺字段注释、是否存在状态/软删/时间字段、是否存在非空业务字段、是否存在长文本字段来实时输出针对性提示。
- 提醒项现已支持风险分级：阻断问题使用红色（danger），建议补齐使用黄色（warning），增强型建议使用蓝色（advice），已满足条件/普通说明保持默认样式（info），用户可更快区分“必须修”和“可以后补”。
- 按最新交互反馈，字段区提醒入口已从圆角图标按钮改成真正的三角告警触发器：视觉上更接近 `⚠️` 语义，避免看起来像普通方形/圆形按钮。
- 进一步微调后，三角告警内的图形已从嵌套 warning icon 改成纯 `!` 标记，避免“三角里再套一个三角”的视觉冲突，整体更干净。
- 最终提醒入口已回退为更稳妥的圆形提示按钮；同时，历史记录不再只显示“哪张表”，而是会展示表名→实体名、模块/业务名、权限前缀和保存时间，并明确这是“点击可恢复整套配置”。
- 已定位历史记录为空的根因：此前只有手动点击“保存配置”才会写入 `localStorage`，用户完成“生成预览 / 下载 ZIP / 写入项目”并不会自动落历史。现已改为成功预览、下载、写入后自动写入历史，更符合自然操作路径。
- 代码预览区已补齐“当前文件用途说明”：会按文件路径动态识别后端控制器、服务、Mapper、DTO、前端页面、前端脚本、SQL 补丁等类型，并提示当前文件最应该检查的点。
- 已定位生成器模块“展示不全”的根因：`GeneratorView.vue` 仍在用 `min-height: calc(100vh - 250px)` 强顶整个双栏内容区高度，工具栏增高后会把整个模块继续往下挤。现已改为复用现有列表页模式，让外层模块完整可见，仅内部表格/代码区域滚动。
- 代码预览 tabs 的左右滚动按钮此前直接沿用 Element Plus 默认浅灰色，在深色代码区里对比度明显不足。现已补齐专用高对比样式（深底、亮字、边框与 hover 态），确保深色主题下也能明显看出“左右移动文件”入口。
- 已顺手清掉 `MenusView -> StatusTag` 的既有 warning：问题来自部分菜单节点缺失 `status/visible` 字段，现已在菜单树加载后统一补默认值，避免未定义值继续传入 `StatusTag`。
- 代码预览区现已支持“复制路径 / 复制内容”：优先使用 `navigator.clipboard.writeText`，不可用时再回退到隐藏 textarea 复制，保持浏览器兼容性。
- 代码预览区现已支持阅读模式切换：默认保留长行并走横向滚动，需要时可切到“自动换行”，更适合在窄屏或长字段模板里连续阅读。
- 已补齐生成器前端参数校验：对表名、模块名、业务名、实体名、权限前缀按后端同规则校验，并在预览/下载/写入前统一执行 trim 规范化后透传。
- 已完成写入安全交互：生成器写入遇到“已存在文件”时会弹覆盖确认框，只有用户确认后才会携带 `overwrite=true` 重试写入，避免误覆盖。
- 已将写入冲突流程升级为“先查询后确认”：写入前先调用冲突清单接口，确认弹窗会直接展示将被覆盖的文件路径列表（含数量与截断）。
- 已将覆盖确认升级为清单面板：支持滚动浏览全部冲突文件，并可一键复制冲突路径清单，便于先评估再覆盖。
- 已继续增强冲突面板可读性：支持按目录分组展示、关键字筛选与筛选计数反馈，冲突较多时更容易定位关注文件。
- 已新增冲突快捷过滤与分组折叠：支持“高风险 Java（src/main/java）”等一键过滤，并可按目录折叠浏览，减少确认覆盖时的信息噪音。
- 已新增冲突差异视图：支持“仅文件名（忽略路径）”模式，并增加目录统计摘要条，便于快速判断冲突集中区域。
- 已新增冲突风险排序与批量折叠控制：分组按风险优先级排序，并支持一键展开/折叠，减少高冲突场景下的操作成本。
- 已新增冲突清单 TXT 导出：可按当前筛选与显示模式直接下载清单文件，便于变更评审、留档与离线沟通。
- 已新增冲突清单 Markdown 导出：支持带上下文信息（过滤条件、目录统计、分组明细）导出，便于在评审文档中直接复用。
- 已补充 Markdown 风险标签：导出明细与分组均带 `HIGH/MEDIUM/LOW` 标记，并附风险分布统计，便于优先审查高风险冲突。
- 已新增 Markdown“覆盖建议顺序”章节：按风险与目录给出建议复核顺位，帮助评审时先看关键冲突。
- 已新增 Markdown 导出前预览：支持在页面内先查看最终导出内容并复制，减少“导出后才发现格式不对”的往返。
- 已新增生成标记前端闭环：写入成功提示显示 `markerId` 并记录本地最近标记，后续可在“更多 -> 快速删除”中按标记一键回滚。
- 已补齐旧后端兼容：若 `write/conflicts` 接口不存在（404），前端自动降级到旧冲突流程，避免页面直接报错中断。
- 已将“快速删除”标记来源切到后端写入记录接口，确保仅展示真实写入过的记录；后端不可用时再回退本地缓存。
- 已新增“写入记录”列表弹窗：支持按表名和时间范围筛选，并可在记录行内按 marker 一键删除。
- 已新增字典管理页 `src/views/system/DictsView.vue`，当前采用“字典类型 + 字典数据”双区块结构，避免拆成两个独立页面增加切换成本。
- 已新增前端字典 API：字典类型 CRUD、字典数据 CRUD 与按类型查询。
- 已在路由与侧边栏补齐字典管理入口：`/system/dicts`，权限标识使用 `system:dict:list`。
- 字典类型表单当前与后端同构校验：字典类型仅支持小写字母、数字和下划线，且需以字母开头。
- 字典数据区会跟随当前选中字典类型加载，支持从类型列表“查看数据”直接切换到对应数据集。
- 已新增 `src/composables/useDictOptions.js`，统一承载按 `dictType` 加载字典项、读取文案与颜色类型的轻量逻辑。
- 用户/角色/菜单/部门页的状态下拉与状态展示已接入字典项，不再直接写死“启用/禁用/停用/显示/隐藏”。
- 菜单页当前同时消费两类字典：`sys_common_status`（状态）与 `sys_menu_visible`（可见性）。
- 部门页当前消费 `sys_dept_status`，保持“停用”文案与其他模块的“禁用”区分。
- 登录日志页已接入 `audit_exec_status` 与 `audit_login_type`，状态与登录方式筛选/展示改为读取字典项。
- 操作日志页已接入 `audit_exec_status`，状态筛选与展示不再硬编码成功/失败。
- 已新增参数设置页 `src/views/system/ConfigsView.vue`，当前支持按参数键/名称、值类型、内置来源筛选。
- 已补齐前端参数 API：参数列表、新增、编辑、删除。
- 已在路由与侧边栏补齐参数设置入口：`/system/config`，权限标识使用 `system:config:list`。
- 参数键前端校验与后端保持一致：需以小写字母开头，后续可包含小写字母、数字、点、横线与下划线。
- 内置参数在参数页中禁止删除，编辑时参数键不可修改，避免误伤默认配置键。
- 参数页已新增分组列与分组筛选，支持快速区分如 `UI`、`AUTH` 等配置域。
- 参数新增/编辑弹窗当前支持选择已有分组，也支持直接创建新分组值。
- 参数页已新增“历史”入口，可查看单个参数的变更记录列表。
- 当前历史弹窗展示变更类型、分组、参数值、说明与时间，先满足排查和回看需求。
- 历史弹窗已新增“回滚”操作，当前仅允许回滚到非删除态历史快照。
- 回滚成功后会同时刷新参数列表与当前历史列表，便于立即确认回滚结果。
- 历史弹窗已新增“对比”入口，可查看当前参数与指定历史快照的字段级差异。
- 当前 diff 视图首版聚焦关键字段：名称、分组、参数值、值类型、内置状态。
- 参数页已新增“恢复已删”入口，可查看当前仍未恢复的删除记录并直接恢复。
- 已删除参数恢复列表与后端新接口对齐：仅展示尚未恢复的 `DELETE` 历史，恢复成功后会自动从列表移除。
- 参数页历史弹窗已新增“设为基准”能力；选中一条历史后，再点另一条记录的“对比”即可进入“双历史”模式。
- diff 弹窗当前会根据 `compareWithCurrent` 自动切换标题和列名，避免“历史 vs 历史”时继续误显示“当前值”。
- 已删参数恢复列表已新增“预览”入口；用户可在恢复前先查看参数名称、键、分组、值类型、参数值与删除说明。
- 恢复预览弹窗现已补充同键占用提示；若当前已有同键参数，会显示字段级差异并禁用确认恢复。
- 参数页值类型当前已优先消费 `sys_config_value_type` 字典 options；当后端尚未补齐字典时，前端仍会退回本地兜底选项，避免页面不可用。
- 字典管理页颜色类型当前已优先消费 `sys_dict_color_type` 字典 options；列表展示不再直接回显原始值，而是展示字典文案和对应标签类型。
- 参数页来源当前已优先消费 `sys_config_source` 字典 options；筛选、表单、列表标签与恢复预览来源文案已统一走字典配置。
- 字典管理页状态当前已优先消费 `sys_common_status` 字典 options；筛选、表单和状态标签颜色/文案已统一走字典配置。
- 角色管理页依赖 `computed` 生成状态字典 options；若遗漏 `vue` 导入，页面会在渲染阶段直接报错打不开。
- 字典管理页的“字典类型/当前类型说明”在深色主题下更适合加轻量背景、边框和更强对比色，而不是只靠弱化副标题色裸文本展示。
- 最终更稳妥的方式是：浅色主题继续沿用原有变量，深色主题单独提亮该页标题/说明文字，而不是全局一刀切改所有主题颜色。
- 如果深色主题仍发灰，问题往往不只在文字色本身，还在卡片玻璃层透明度过高；此时应局部提高该页卡片底色不透明度，而不是继续无限加深文字色。
- 字典管理真实浏览器回归发现：字典数据颜色为“默认”时，`DictsView` 会向 `ElTag` 传空字符串 `type`，触发 Element Plus prop warning。
- 已将字典颜色标签兜底改为 `undefined` 而非空字符串，字典页真实回归后控制台 warning 已清零。
- 参数设置真实浏览器回归发现：已删参数列表首次打开会命中后端 `config_key` collation 混用报错；后端修复后，恢复预览与恢复确认链路均已回归通过。
- 参数页真实回归已确认“双历史”模式可用：先设一条历史为基准，再对另一条历史执行 diff，前端标题与列名会正确切到“历史值A / 历史值B”。
- 生成器真实回归已确认“写入项目 → 写入记录 → 删除”可用：写入成功提示会返回 marker，写入记录弹窗可直接按 marker 删除并清空本次记录。
- 生成器冲突确认真实回归发现：`GeneratorView` 冲突快捷过滤仍在使用 `el-radio-button label` 充当值，触发 Element Plus 3.0 兼容 warning。
- 已将冲突快捷过滤切换为 `value` 写法，生成器冲突弹窗复测后控制台 warning 已清零。
- 字典页与参数页真实回归已确认：单条数据场景下查询、重置、分页区每页条数切换均可用，且临时数据已清理。
- 前端角色页数据范围现已与后端扩展对齐，新增 `DATA_SCOPE_DEPT_AND_CHILD = 2`，角色表单可直接选择“本部门及以下数据”。
- 角色列表数据范围标签现已支持展示“本部门及以下”，避免后端返回新范围时前端仍误显示为“全部数据”。
- 真实浏览器回归已确认：角色编辑弹窗中可直接选择“本部门及以下数据”，保存后列表标签同步显示“本部门及以下”。
- 本轮回归已将测试角色恢复为原始“本部门数据”，并确认角色页控制台 warning/error = 0。
- 结合真实后端进一步验证后可确认：当 `dept_scope_role` 临时切到“本部门及以下”时，`dept_user` 可见子部门 `outside_user`，但不可见临时创建的树外根部门用户。
- 该轮联调通过真实 API 完成临时部门/用户搭建与回收，结束后角色配置、用户列表、部门树均已恢复原始基线。
- 后端现已补齐部门子树与树外边界单测，前端后续联调可直接复用这套“子部门可见、树外不可见”的稳定回归前提。
- 真实前端用户页回归已确认：`dept_scope_role` 临时切到“本部门及以下”后，`dept_user` 登录用户页可见 `outside_user`，页面总数为 6 条。
- 本轮用户页回归控制台 warning/error = 0，结束后已恢复角色原始配置，未污染联调基线。
- 当前 token 消耗升高的主要原因来自双端 planning 文件持续膨胀，而续开发流程此前会默认读取双端完整 planning；对前端普通任务来说，这些历史内容大多不是首轮必需上下文。
- 本轮已新增 `jianqing-admin-web/current_state.md`，并配合工作区协议改为“轻量摘要优先、按侧展开、必要时再读完整 planning”，可在不牺牲联调质量的前提下降低对话上下文成本。

## Technical Decisions
| Decision | Rationale |
|----------|-----------|
| 使用 Vue3 + Element Plus | 快速构建管理台并保持组件一致性 |
| 使用 Hash 路由 | 简化本地与静态部署场景 |
| 使用 axios 拦截器处理认证 | 统一处理 token 注入与 401 退出 |
| 关键/复杂逻辑写必要注释 | 保持代码易读且避免过度注释 |
| 系统管理页采用前端搜索+分页 | 降低后端分页改造成本，先保证可用体验 |
| 操作按钮按权限显隐 | 提升前端交互安全性与角色体验一致性 |
| 前端优先简单易读实现 | 保持可维护性并降低学习成本 |
| 前端 API 调用仅使用 `GET` / `POST` | 与后端接口约束保持一致，避免联调歧义 |
| 列表页统一分页布局（total/sizes/pager） | 提升长列表可读性与浏览效率 |
| 分野筛选尽量走后端查询参数 | 保证筛选结果与分页总数一致，避免“只筛当前页”偏差 |
| 可读性优先通过结构与常量表达 | 让代码先自解释，再只为复杂边界补必要注释 |
| 前端协作规范文档化 | 降低后续 AI/人工接手时的上下文恢复成本 |
| 优先做路由级拆包与组件按需引入 | 以最小改动降低首屏包体积与构建告警 |
| 列表页优先提供明确反馈文案与按钮 loading | 用最小交互成本提升操作可感知性，避免重复提交 |
| 重复异步反馈优先抽轻量 composable | 在不引入复杂抽象前提下收敛 loading/空态样板代码 |
| 成功反馈文案优先复用统一工具 | 保持交互语义一致，避免页面各自拼接提示文本 |
| 全局 locale 优先通过根组件配置 | 避免按需引入或插件调整时出现分页/选择器文案回退 |
| 数据权限首批仅暴露后端已落地范围 | 保持前后端语义一致，先完成最小闭环再扩展更多选项 |
| 部门管理先采用树表 + 轻表单 | 先做最小可用部门树维护，避免过早引入复杂拖拽/级联编辑 |
| 用户表单优先显示真实部门名称 | 让部门字段具备业务语义，减少手填 ID 带来的误操作 |
| 部门负责人优先显示真实用户身份 | 让组织信息具备可读性，避免负责人字段再次退回为纯数字 |
| 列表筛选优先采用显式查询/重置 | 减少输入中途抖动，统一不同页面的筛选心智 |
| 列表区优先固定工作区高度 | 保持页面骨架稳定，数据增减只影响表格内部滚动，不再推高整页 |
| 关键弹窗统一 append-to-body | 避免被表格固定列、卡片容器或布局层级遮挡 |
| 系统管理同类列表优先统一筛选交互 | 避免用户在用户/部门/角色/菜单页来回切换时出现操作心智不一致 |
| 审计页也保持同样的查询/重置节奏 | 保证跨模块操作习惯一致，不因前后端分页差异破坏使用体验 |
| 列表表格优先使用固定 `height` 而非 `max-height` | 让空数据/少数据场景也占满工作区，保持整体布局稳定 |
| 响应式布局优先通过共享样式与 composable 收口 | 减少各页面重复算高、重复写断点样式，统一宽高适配行为 |
| 小屏分页优先允许横向滚动并靠左对齐 | 保证 640px 级别宽度下分页仍可操作，不强行压缩控件宽度 |
| 列表区继续采用“表格面板 + 分页面板”双卡片 | 强化信息层次，让表格主体与翻页操作视觉解耦 |
| 列表顶部不再重复展示页面标题 | 左侧菜单已提供当前位置，内容区优先留给筛选与操作模块 |
| 列表布局优先 `flex` 结构，不再依赖魔法高度参数 | 减少反复微调 `offset` 造成的维护负担，保持结构稳定与可读性 |
| 高重复页面优先抽轻量 composable 收敛 | 先减样板和重复状态逻辑，再做组件级拆分，避免一次性大改风险 |
| 弹窗表单先抽状态层，不抽业务提交层 | 保持校验、接口调用与领域差异留在页面内，降低通用层失控风险 |
| 删除链路先抽操作编排层 | 统一确认/执行/刷新/提示节奏，实体差异通过参数注入保持轻量 |
| 保存链路只抽固定收尾节奏 | loading、关闭、刷新、提示可复用，字段校验与 create/update 分支仍留页面内 |
| 列表页头部工具栏统一走共享组件 | 保持系统页与审计页骨架一致，避免后续页面风格分叉 |
| 第二次出现重复时就评估抽共享层 | 尽早收敛重复逻辑，避免样板代码重新扩散 |
| Dashboard 统计逻辑优先抽组合式工具 | 保持页面只负责展示与绑定，权限判断/请求编排留在 composable |
| 页面初始化优先复用共享收口工具 | 避免各页重复书写 `onMounted + try/catch + ignoreHandledError` 样板代码 |
| 状态展示优先复用共享标签组件 | 保持系统页与审计页状态语义、色彩与文案表达一致 |
| 列表基础能力优先统一到底层 composable | 先收敛分页/查询/刷新基础节奏，再由上层 wrapper 保留本地/远程语义差异 |
| 代码生成器页首版先追求可用与一致性 | 先复用现有管理台骨架，不为低频工具页额外发明复杂交互模型 |
| 生成器参数规则前后端保持同构 | 减少“前端可填但后端拒绝”的来回重试，提高预览/下载链路稳定性 |
| 写入冲突必须二次确认 | 把“覆盖已有文件”变成显式用户决策，降低误操作风险 |
| 覆盖确认优先展示具体文件清单 | 风险可视化比泛化提示更有效，便于快速判断是否继续覆盖 |
| 冲突清单支持复制导出 | 降低人工抄录成本，便于代码评审或变更前沟通 |
| 大批量冲突优先“分组 + 搜索” | 缩短人工扫描路径，降低覆盖确认决策成本 |
| 高风险目录提供快捷过滤入口 | 优先审查关键目录冲突，提升覆盖确认效率 |
| 提供“路径/文件名”双视角 | 同时支持精确覆盖审查与快速重复文件名识别 |
| 冲突展示默认按风险优先排序 | 先审查高风险目录，降低误覆盖关键代码概率 |
| 冲突清单支持文件导出 | 覆盖前可沉淀审阅证据，提升协作透明度 |
| 导出优先保留上下文元信息 | 让清单在离开页面后仍可被独立理解与复核 |
| 导出内容应体现风险优先级 | 让评审者优先处理高风险冲突，降低误覆盖关键代码概率 |
| 导出内容应提供可执行建议顺序 | 从“看见风险”升级到“知道先看什么” |
| 导出前支持页面内预览 | 先确认内容再落盘，降低反复导出成本 |
| 标记删除入口放在生成器工具页内部 | 保持“生成-回滚”操作闭环，减少跨页面查找成本 |
| 已写入记录优先走服务端 | 保证跨设备/跨会话可追踪，未写入草稿仍保持本地轻量存储 |
| 写入记录列表内聚筛选与删除 | 减少用户在“查记录-删记录”间的操作跳转 |
| 字典管理首版采用单页双区块 | 保持“先选类型，再管数据”的连续心智，避免页面来回跳转 |
| 字典消费首批优先替换高频状态字段 | 先消除系统页硬编码选项，再逐步扩展到审计与业务表单 |
| 审计页字典消费优先覆盖筛选项与表格标签 | 先统一用户可见文案，再考虑更深层的数据枚举模型改造 |
| 参数设置首版先做单页列表 CRUD | 先补齐可维护性与动态发布闭环，再考虑分组、历史版本与灰度配置 |
| 参数分组首版先做轻字段 | 先满足筛选与发布分组需求，避免过早引入独立分组管理复杂度 |
| 参数历史首版先做只读列表 | 先满足可追溯性，再考虑版本 diff、回滚与审批链路 |
| 参数回滚首版先限制删除态 | 先保证常见“值回退”闭环，删除恢复后续单独设计 |
| 参数 diff 首版先做字段表格对比 | 先满足回滚前人工确认，再考虑更复杂的文本级高亮 diff |
| 参数删除恢复先做独立弹窗入口 | 已删除参数不再出现在主列表与当前历史弹窗中，单独入口更直白且改动更小 |
| 参数多版本 diff 先走“选一条做基准” | 避免一次性引入双选器/复杂表单，在现有历史表格内即可完成最小交互闭环 |
| 参数恢复前预览先做只读详情弹窗 | 先帮助用户确认恢复对象，无需过早引入更重的差异对比界面 |
| 参数恢复前差异只在冲突时展示 | 无冲突时直接恢复更顺畅，有冲突时再用差异表格承载风险说明 |
| 字典消费扩面先保留本地兜底 options | 既能逐步去掉硬编码，又不会因字典 seed 缺失阻断页面使用 |
| 字典颜色展示应同步消费文案与 tag 类型 | 避免只替换下拉来源却仍在列表中回显原始值，造成体验割裂 |
| 参数来源字典消费要同时兼顾筛选值类型转换 | 字典接口返回字符串值，前端筛选与表单仍需转换为数字，避免过滤失效 |
| 通用状态字典扩面优先复用 `StatusTag` 外壳 | 只替换其文案与颜色来源，能以最小改动收口现有状态展示链路 |
| 字典颜色标签兜底不再传空字符串给 `ElTag` | 当前 Element Plus 对 `type` 校验更严格，默认色场景应走未传值语义而非 `type=''` |
| 生成器冲突快捷过滤统一使用 `el-radio-button value` | 避免继续依赖即将废弃的 `label=value` 语义，减少 Element Plus 升级噪音 |
| 数据范围扩面先只补角色页选项与展示 | 当前后端能力已具备，前端最小闭环只需消除“无法选择/展示错误”即可，避免额外引入无关交互复杂度 |

## Resources
- `src/router/index.js`
- `src/api/http.js`
- `src/views/LoginView.vue`
- `src/views/DashboardView.vue`
- `src/views/system/UsersView.vue`
- `src/views/system/RolesView.vue`
- `src/views/system/MenusView.vue`
- `src/views/audit/OperLogsView.vue`
- `src/views/audit/LoginLogsView.vue`
- `src/composables/useAsyncState.js`
- `src/utils/feedback.js`
- `src/views/LoginView.vue`
- `src/layouts/MainLayout.vue`
- `src/App.vue`
- `src/views/system/RolesView.vue`
- `src/constants/app.js`
- `src/views/system/DeptsView.vue`
- `src/router/index.js`
- `src/layouts/MainLayout.vue`
- `src/api/system.js`
- `src/views/system/UsersView.vue`
- `src/views/system/DeptsView.vue`
- `src/views/system/RolesView.vue`
- `src/views/system/MenusView.vue`
- `src/views/audit/OperLogsView.vue`
- `src/views/audit/LoginLogsView.vue`
- `src/views/system/RolesView.vue`
- `src/views/system/MenusView.vue`
- `src/layouts/MainLayout.vue`
- `src/router/index.js`
- `src/views/system/MenusView.vue`
- `src/views/system/RolesView.vue`
- `src/stores/auth.js`
- `src/api/system.js`
- `src/views/audit/OperLogsView.vue`
- `src/views/audit/LoginLogsView.vue`
- `docs/FRONTEND_CONVENTIONS.md`
- `src/constants/app.js`
- `src/utils/validators.js`
- `src/composables/usePermissions.js`
- `src/router/index.js`
- `src/main.js`
- `vite.config.js`
- `index.html`
- `public/favicon.svg`
- `src/views/system/UsersView.vue`
- `src/views/system/RolesView.vue`
- `src/views/system/MenusView.vue`
- `src/views/audit/OperLogsView.vue`
- `src/views/audit/LoginLogsView.vue`

## Cross-Project Sync
- 若接口字段或认证策略变更，需要同步更新 `jianqing-backend/progress.md` 与本文件。
